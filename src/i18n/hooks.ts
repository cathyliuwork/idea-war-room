/**
 * i18n Hooks
 *
 * Re-exports hooks from context for convenience.
 * Also provides additional utility hooks.
 */

export { useLanguage, useTranslation } from './context';

// Re-export types
export type { Language, LanguageContextType } from './types';
